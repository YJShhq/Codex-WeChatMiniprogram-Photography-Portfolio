# 成木汤汤作品更新说明

## 当前版本：本地自助更新

1. 把新照片裁成竖图或方图，放入 `待处理图片` 文件夹。
2. 运行项目根目录的 `压缩并导入图片.ps1`。
3. 在 `data/works.js` 复制一条作品记录，修改图片路径、分类、标题和说明。
4. 在微信开发者工具中点击“编译”；确认后上传新版本。

脚本会把长边缩至 1200 像素，并自动降低质量，确保单张图片不超过约 185 KB。

## 手机后台上传

当前项目已绑定云环境 `cloud1-d3gl5lk95dc8aebc2`，并包含手机作品管理页。

首次部署：

1. 在微信开发者工具中打开“云开发”，确认当前环境为上述环境。
2. 在云数据库中新建 `works` 和 `admins` 两个集合；客户端权限均设为不可直接读写，数据操作统一经过云函数。
3. 在项目树中找到 `cloudfunctions/portfolio`，右键选择“上传并部署：云端安装依赖”。
4. 重新编译，进入“联系”页最下方的“作品管理”，复制页面显示的 OpenID。
5. 在 `admins` 集合新增一条记录，字段名为 `openid`、类型为字符串、值为刚复制的 OpenID。
6. 重新进入作品管理页，即可从相册选择、压缩、上传、上架、下架或删除作品。

云数据库只保存标题、分类、说明和云文件 ID；图片本体存放在云存储，不进入小程序代码包。
